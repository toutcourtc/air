{% block header %}
        <header>    
        {% block navbar %}
            <div id="navbar">
                <div class="menu">
                    <div>
                        <a href="/" style="margin : 0 10px; padding : 2px;"><i class="las la-home icons"></i></a>
                    </div>
                    <div class="dropdown">
                        <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown">
                            Vols
                        </button>
                        
                        <ul class="dropdown-menu btn btn-primary">
                            <li><a href="/vol" class="dropdown-item btn btn-primary">Voir</a></li>
                            {% if app.user and "ROLE_ADMIN" in app.user.roles %}
                            <li><a class="dropdown-item btn btn-primary" href="/vol/new">Ajouter</a></li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="dropdown">
                        <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown">
                            Les villes desservies
                        </button>
                        <ul class="dropdown-menu btn btn-primary">
                            <li><a href="/ville" class="dropdown-item ">Voir</a></li>
                            {% if app.user and "ROLE_ADMIN" in app.user.roles %}
                            <li><a class="dropdown-item" href="/ville/new">Ajouter</a></li>
                            {% endif %}                   
                            
                        </ul>
                    </div>
                </div>
                <div class="log">
                    {% if app.user %}
                        {{app.user.email}}
                        <a href="/logout"><i class="las la-sign-out-alt icons"></i></a>
                        {% else %}
                        <a href="/login"><i class="las la-sign-in-alt icons"></i></a>
                        <a href="/register"><i class="lar la-user-circle icons"></i></a>
                    {% endif %}
                    
                    
                </div>
                
            </div>
        </header>
        {% endblock %}
    {% endblock %}